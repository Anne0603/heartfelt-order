# 心意訂單管理系統 — 開發紀錄

> 這份文件用來持續記錄每次審視程式碼時發現的問題、討論過程、處理狀態。
> 每次有新進展會同步更新，開發告一段落後會整理成正式的完整版總覽文件。

## 狀態圖例
- ✅ 已完成
- 🔜 已排入計畫，尚未動工
- 💬 已討論，待使用者決定是否要做
- ⚠️ 待使用者自行處理（例如需要登入外部後台操作）

---

## 2026-08-29　全站視覺放大 + 登入機制修正

- ✅ 首頁移除「今日待辦」清單，改為統計卡片＋快速操作區的簡潔版面
- ✅ 首頁統計卡片、快速操作整體放大（字級、圖示、padding）
- ✅ 首頁快速操作區由三欄兩排改為兩欄三排，並確認最小手機寬度(320px)下不會裁切
- ✅ 側邊欄整體放大（含選單開關按鈕、選單項目文字、使用者資訊、登出按鈕）
- ✅ 修正全站所有 `<input>`/`<select>`/`<textarea>` 字級不足16px，導致 iOS Safari
  點擊輸入框時整頁自動放大縮放的問題（統一調整為16px以上）
- ✅ 修正手機版側邊欄在 collapsed 狀態下，「重新整理」/「登出」按鈕文字消失的問題
  （強制顯示規則漏寫 `.btn-logout-label`）
- ✅ 修正側邊欄收合狀態（68px）在放大版面後圖示/頭像被擠壓變形的問題
  （加大收合寬度至88px，並補上 box-sizing: border-box）
- ✅ 登入流程改為自動容錯：`signInWithPopup` 被瀏覽器封鎖或使用者關閉時
  （`auth/popup-blocked`、`auth/popup-closed-by-user`），自動改用
  `signInWithRedirect` 整頁轉跳，並正確接住 `getRedirectResult()`，
  解決 iPhone Safari 封鎖彈出視窗時卡住的登入問題

## 2026-08-29　專業視角全面體檢 + 訂單快取優化

針對整個系統做了一次架構/安全/效能審視，發現以下項目：

### 🔴 安全性（最優先）
- ⚠️ **GitHub Token 明文寫在進度文件裡，且設定永不過期**
  → 需使用者自行到 GitHub 後台撤銷並更換為有效期限、範圍限縮的新 token
- ⚠️ **Cloudinary 為無簽章上傳（Unsigned Preset），存在被盜刷流量風險**
  → 需使用者自行到 Cloudinary 後台幫 preset 加上檔案類型/大小/資料夾限制

### 🟡 效能與擴充性
- ✅ **訂單列表頁「一次全撈」問題** → 改成預設只抓近3個月訂單，資料庫端就
  縮小查詢範圍（不是抓全部再篩），並提供「顯示全部歷史訂單」按鈕手動展開；
  同時處理了以下邊界情況，避免「省流量」反而讓資料悄悄消失：
  - 從首頁/通知鈴鐺帶著篩選條件（逾期、待處理、搜尋）進來的一律載入全部歷史
  - 使用者手動把篩選切成「待處理」「已逾期未出貨」「已出貨但未收款」，
    或日期篩選選到3個月以前，都會自動展開抓取全部歷史
  - 匯出 Excel 時，如果選的範圍超出目前已載入的3個月，會先在背景補抓
    完整資料再匯出，避免匯出一份悄悄缺資料的檔案
  - 首頁統計、通知鈴鐺、報表、利潤總覽、客戶詳細頁的訂購紀錄，都沒有動到，
    繼續抓取完整歷史資料，維持原本的準確性
- ✅ **同一批訂單資料被重複抓取**（首頁統計 + 通知鈴鐺各自呼叫一次 listOrders）
  → 已加入 30 秒短期記憶體快取（`js/orders.js` 的 `listOrders()`），
  同一段時間內重複查詢會直接沿用快取結果，不重新打資料庫；
  任何寫入操作（新增/編輯/收款/出貨/作廢/永久刪除，共8個函式）
  都會立即清空快取，確保資料異動後畫面永遠看到最新狀態
- 💬 **首頁統計/通知鈴鐺尚未改用聚合查詢，使用者決定先擱置**（2026-08-29 討論）：
  深入評估後發現這不只是「建一個索引」的小事，而是要先做資料結構異動——
  Firestore 規定「日期範圍」跟「排除某個狀態」不能用在不同欄位的查詢條件裡，
  「已逾期未出貨」這個統計天生需要這兩種條件，所以必須先在每張訂單多存一個
  「目前是否算使用中」的欄位（例如 isActive），查詢才會合法、才能建索引。
  而且舊訂單資料庫裡沒有這個欄位，必須先跑一次性補值程式，否則舊訂單會在
  統計裡「憑空消失」——這對「抓被遺忘的舊逾期單」這個功能來說是最危險的
  副作用。整套是「改資料結構＋補舊資料＋建索引＋改查詢邏輯」，不是單一步驟。
  使用者確認目前用法沒有感覺到變慢，決定先擱置，不現在做。
  → **之後如果要重啟這個項目，直接參考這一段的技術評估即可，不用重新分析。**
- 💬 **商品清單「一次全撈」尚未處理**：商品項目數量通常比訂單少很多，
  暫時優先度較低，之後有需要再討論

### 🟢 工程維護面
- 💬 版本快取號目前靠人工遞增，容易漏改；可考慮寫成一鍵部署腳本或
  GitHub Actions 自動化（尚未實作，優先度較低）
- 💬 沒有自動化測試、沒有預覽環境，屬於小型系統的合理取捨，
  但改動大功能時建議上線前先用不同角色帳號實際測試一輪

### 🔵 商業邏輯／資料面
- ✅ **第二位 superadmin 備援**：原本「可指派角色」選單刻意不含超級管理員
  （保守設計，避免誤開最高權限給不該開的人），使用者明確表示只是要幫
  自己開備援帳號、不開放給別人，已加回這個選項。操作方式：用第二個
  Google 帳號登入一次（會卡在審核中畫面），再用原帳號到「待審核申請」
  核准、角色選「超級管理員」即可，全程在前台操作，不用碰資料庫

---

## 討論記錄：訂單/商品清單分頁化

**問題：** `listOrders()`、商品清單目前都是 `getDocs()` 一次撈出全部文件，
沒有使用 `limit()` / `startAfter()` 做游標分頁，訂單量成長到幾千筆後
會拖慢載入速度、增加 Firestore 讀取成本。

**已實作（2026-08-29）：訂單列表頁預設近3個月範圍**
沒有採用「一次抓30-50筆、捲到底載入下一批」的傳統游標分頁，因為訂單列表頁
的搜尋/篩選（訂單編號、客戶、電話、日期區間、出貨狀態）目前都是在瀏覽器端
針對已下載的資料做篩選，如果改成游標分頁，篩選功能只能搜到「已經載入的那幾批」，
搜尋準確度會出問題。改用風險較低的做法：查詢時直接在 Firestore 端加上
「日期 ≥ 3個月前」的條件，一次抓進來的資料量自然變少，篩選功能完全不受影響，
需要看更早資料時再讓使用者主動展開。

**尚未實作／可再討論的方向：**
1. **首頁統計數字／通知鈴鐺**：不要抓整份訂單文件回來自己數，
   改用 Firestore 的聚合查詢 `count()`（搭配 `where` 條件）直接在
   資料庫端算出數量，前端不需要下載完整資料。技術卡點：`count()`
   搭配「日期範圍」+「排除某個狀態」這種跨欄位條件時，Firestore
   不允許這樣查詢，必須先在訂單多存一個「是否使用中」的欄位
   （例如 isActive）才能查，而且舊訂單需要先跑一次性補值程式，
   否則舊訂單會在統計裡「憑空消失」。完整評估見上方
   「首頁統計/通知鈴鐺尚未改用聚合查詢」條目。
   **2026-08-29 使用者決定：目前用法沒感覺到變慢，先擱置不做**
2. **商品清單分頁化**：商品項目數量通常遠少於訂單，優先度較低
3. **進階選項（複雜度較高，非必要）**：改成「異動時就同步更新一份統計文件」
   的寫法，讀取時直接讀那份摘要文件，完全不用查詢原始訂單集合。
   效能最好但複雜度高，建議資料量真的成長到有感變慢時再考慮

---

## 2026-08-29　移除成員後未即時登出的修正

- ✅ **修正「被移除後沒有立刻被踢出畫面」**：原本成員被移除/角色被拿掉時，
  如果他當下正在使用網頁，系統不會主動反應，要等他自己重新整理頁面才會
  發現。現在改成即時監聽自己的成員資料（`js/auth.js` 的
  `watchMembershipLive`），一旦被移除、角色被清空、或狀態變成非
  active，會立刻自動登出並跳出提示告知原因，不用等使用者自己重新整理，
  也不會讓他停留在已經失效的畫面上操作到一半才發現全部失敗
  （附帶好處：角色被即時調整時，暫存資料也會同步更新）
- 💬 **暱稱資料在成員被移除後不會一起清掉**（`publicProfiles` 集合），
  屬於保留特色而非漏洞，若之後想改成「移除時一併清空暱稱」可再討論，
  優先度低，暫不處理

## 2026-08-29　一鍵備份 + PWA（加到主畫面）

- ✅ **一鍵備份**：側邊欄新增「資料備份」（超級管理員限定），一鍵把訂單、
  商品、進貨/領用/盤點記錄、客戶、分類、單位、支出、操作紀錄、成員
  共11個集合完整匯出成一個 JSON 檔案下載。已知限制：暱稱資料
  （publicProfiles）因為 Firestore 規則設計上禁止「列出全部」
  （防止有心人士掃出全部成員信箱），沒辦法一併匯出，其餘商業資料
  都完整包含。建議定期（例如每個月）手動下載一次，存在系統以外的
  地方比較安心
- ✅ **PWA（可加到手機主畫面、像 App 一樣開啟）**：新增 `manifest.json`、
  三種尺寸的品牌圖示（沿用登入畫面的「心」字設計）、極簡版
  `service-worker.js`。特別把 Service Worker 設計成**完全不做任何快取**
  ——因為這個專案本來就有「每次部署手動遞增版本號」的機制防止使用者
  看到舊版本，如果 Service Worker 又自己快取一份，兩套機制會互相打架，
  反而更容易卡在舊版本。目前只負責滿足「可安裝」的技術條件，之後真的
  需要離線瀏覽功能再回頭擴充。手機瀏覽器選單裡的「加入主畫面」
  之後就會出現獨立圖示，開啟後沒有網址列，體感更像一個 App

## 2026-08-30　新增退貨流程

- ✅ **退貨功能上線**：訂單詳細頁（限已出貨、未作廢的訂單）新增「登記退貨」
  按鈕，可以選擇要退哪些商品、退幾件、要不要加回庫存，系統自動算出退款
  金額。設計精神比照作廢訂單——不直接改動原始訂單內容，而是新增一筆
  獨立的退貨記錄（`orderReturns` 集合），保留完整歷史軌跡
  - 訂單累加一個 `returnedAmount` 欄位，收款狀態、應收/應退金額計算
    （`getPaymentStatus`、新增的 `getOutstandingBalance`）都會自動扣除
    已退貨金額；如果客戶已經付清、後來又退貨，畫面會顯示「應退還客戶
    $X」而不是誤判成已結清
  - 加回庫存的機制新增獨立函式 `restockFromReturn`（不是共用領用函式
    傳負數這種容易誤讀的寫法），領用記錄列表會顯示「退貨回補 +N」
  - 已加入防呆：退貨數量不能超過「當初賣出數量 － 之前已經退過的數量」
  - 附帶修正一個潛在 bug：`voidRecord`（作廢庫存記錄）原本沒有特別處理
    「退貨回補」這種方向相反的記錄，如果有人把一筆退貨回補記錄作廢，
    庫存會被錯誤地疊加扣兩次，已修正為依 `source` 判斷正確的調整方向
  - 利潤總覽、統計報表原本算營收/客戶消費/收款狀況都是直接用訂單總額，
    沒有扣退貨金額，會虛報數字，已一併修正（訂單層級的營收數字都已經
    正確扣除；商品/分類層級的銷售排行**尚未**扣除，屬於已知限制，
    見下方待辦）
- ⚠️ **需要手動到 Firebase 後台操作**：`firestore.rules` 已經加入
  `orderReturns` 集合的規則（讀取：全部有效成員；新增：管理員以上/
  訂單進出貨人員；不可修改刪除，比照操作紀錄的精神保留稽核完整性），
  **但這份規則只是存在 repo 裡，還沒有真的生效**。需要使用者自己把
  `firestore.rules` 整份內容複製貼到
  [Firebase Console 的 Firestore 規則頁面](https://console.firebase.google.com/project/heartfelt-order/firestore/rules)
  覆蓋貼上，然後按「發布」，退貨功能才能真的運作（不然新增退貨記錄
  時會被規則擋下來，跳出權限錯誤）
- 💬 **已知限制**：退貨如果有加回庫存，理論上當初認列的銷貨成本
  （COGS）也應該跟著沖銷一部分，不然毛利會被低估一點點；另外商品/
  分類銷售排行（`reports-ui.js` 的 topProducts/categoryStats）目前
  還沒扣除退貨數量。這兩個都需要更細緻的資料比對（要把退貨記錄跟
  原始訂單品項一一對應），優先度中等，先記錄下來，之後有空再處理

## 2026-08-30　補上退貨對報表/毛利計算的完整扣除

- ✅ **商品/分類銷售排行扣除退貨數量**：`reports-ui.js` 的商品銷售排行、
  分類銷售占比，原本沒扣退過的商品數量，已修正
- ✅ **毛利計算的成本沖銷**：退貨如果有勾選「加回庫存」，代表商品又
  回到可以賣的狀態，當初出貨認列的成本現在會跟著沖銷一部分（只有
  「加回庫存」的部分才沖銷；商品壞掉沒辦法賣、沒勾加回庫存的話，
  成本繼續照算，不會沖銷）。這個邏輯同時套用到利潤總覽的**總金額**
  跟點進去看的**明細拆解頁**（依商品/依包材項目/依訂單），確保
  兩邊加總起來永遠對得起來，不會出現「總金額對了但明細兜不攏」的情況
- 新增 `js/orders.js` 的 `listAllReturns()`，供報表/利潤總覽這種需要
  跨訂單統計退貨資料的地方共用
- 這兩項原本列的「已知限制」現在都已經處理完畢

## 2026-08-30　全站錯誤訊息改善

- ✅ **權限不足時的錯誤訊息改成看得懂的中文**：原本全站任何操作被
  Firestore 規則擋下來時，都是直接顯示英文技術錯誤
  「Missing or insufficient permissions」，一般人看到只會以為系統壞了，
  不知道其實是「這個角色本來就不該做這個操作」。新增
  `js/utils.js` 的 `friendlyErrorMessage()`，遇到權限被拒的情況會改顯示
  「你目前的帳號角色沒有權限執行這個操作，如果覺得應該要有權限，
  請洽詢管理員確認角色設定。」，網路異常也有對應的中文說明。
  全站（10 個檔案、59 處）顯示錯誤訊息的地方都已經統一套用這個函式

## 2026-08-30　商品列表卡片排版跳行修正

- ✅ **修正商品列表卡片高度不一致的問題**：原本卡片是「左邊照片+名稱、
  右邊庫存/售價」左右並排的排版，但整排設定成「空間不夠就自動換行」，
  結果商品名稱一長，右側的庫存數字就會被擠到下一行，導致每張卡片
  高度忽高忽低、看起來像跳行。修正成右側庫存/售價區塊固定不換行、
  永遠貼齊右上角，左側名稱可以正常換行但不會影響右側位置，
  整份列表卡片高度規律一致

## 2026-08-30　客戶資料的訂購管道改成下拉選單

- ✅ **修正客戶資料頁面「訂購管道」是自由輸入文字框的問題**：訂單建立
  頁面的訂購管道本來就是下拉選單（LINE/IG/FB/現場/其他，選「其他」才
  能自訂文字），但客戶資料頁面卻是純自由輸入，同一個管道容易被打成
  不同寫法（例如「LINE」跟「line」），統計時會兜不起來。已改成跟訂單
  頁一致的下拉選單
  - 把 `ORDER_CHANNELS` 清單抽到 `contacts.js`（資料層）當單一資料
    來源，訂單頁跟客戶資料頁共用同一份，不會各自維護出兩份不一致的清單
  - 把 `openCustomTextModal`（選「其他」時跳出的自訂文字輸入視窗）
    從 orders-ui.js 搬到共用的 `modal-ui.js`，兩邊都能用，不用各自
    複製一份重複程式碼

## 2026-08-30　手機上傳照片偶爾失敗的修正

- ✅ **修正照片上傳（商品照片、支出收據）在手機上偶爾失敗的問題**：
  原因是同一份 Cloudinary 設定資料被連續查詢資料庫兩次——一次是
  UI 層自己先查一次做檢查，另一次是 `uploadImageToCloudinary()` 內部
  真正要上傳時又查一次。手機網路本來就比電腦的 WiFi/有線網路更容易
  有延遲，這兩次獨立查詢中只要有一次卡到、還沒真的讀到資料就被拿去
  用，就會出現「明明設定好了，卻被當作沒設定」的狀況，實際錯誤訊息是
  Cloudinary 回傳的 `Upload preset must be specified when using
  unsigned upload`。已移除多餘的重複檢查，只留 `uploadImageToCloudinary()`
  內部那一次，`items-ui.js`（商品照片）跟 `expenses-ui.js`（支出收據）
  都已修正
- ✅ **順手修正**：Cloudinary 上傳失敗時，原本只顯示籠統的「上傳失敗」，
  現在會把 Cloudinary 實際回傳的失敗原因一併顯示出來，方便之後排查

## 2026-08-30　手機上傳照片失敗的真正原因（修正之前的錯誤診斷）

- ⚠️ **上一次的診斷是錯的**：之前判斷是「Cloudinary 設定被重複查詢兩次
  造成的競速問題」，移除重複查詢後使用者回報「還是一樣」，證實那不是
  真正原因
- ✅ **真正原因：PWA 的 Service Worker 攔截了上傳請求**：稍早新增的
  `service-worker.js` 原本會攔截**所有**網路請求並重新轉發
  （`event.respondWith(fetch(event.request))`），包括上傳照片到
  Cloudinary 這種帶檔案、跨網域的 POST 請求。iOS Safari 在 Service
  Worker 轉發這類「帶檔案的表單資料」請求時，已知有相容性問題，會導致
  表單裡其他欄位（例如 `upload_preset`）在轉發過程中憑空消失，送到
  Cloudinary 時變成空值，跳出「Upload preset must be specified」的
  錯誤——即使 Firestore 裡的設定資料完全正確也一樣會失敗（已請使用者
  截圖確認過設定資料本身沒問題，排除了資料問題的可能）。這也同時
  解釋了為什麼問題只在手機上發生、電腦不會
- ✅ 已修正 `service-worker.js`：改成只攔截「本站同源的 GET 請求」，
  其他一律不介入（不呼叫 `respondWith`，讓瀏覽器原生處理，等同沒有
  Service Worker 存在）。上傳照片這種帶檔案的請求、打到 Cloudinary/
  Firebase 這種外部網域的請求，完全不會再經過 Service Worker
- 💡 **經驗教訓**：這次診斷繞了一圈才找到真正原因，之後如果再遇到
  「只有手機會、電腦不會」且牽涉到網路請求的怪異問題，會優先檢查
  Service Worker 是否介入其中，而不是先假設是資料層或競速問題

## 2026-08-30　輸入框自動放大問題徹底根治

- ⚠️ **之前的修法不夠徹底**：先前只針對「當時找到的」輸入框逐一補上
  `font-size:16px`，但沒有涵蓋全站，後來使用者在「分類管理」頁面的
  「新增分類名稱」輸入框又踩到同樣的自動放大問題（那幾個輸入框是
  後來才加的，當時沒被抓到）
- ✅ **改用根本解法**：在 `css/style.css` 最基礎的全域樣式加上一條
  保底規則——所有 `<input>`（文字/信箱/數字/日期/電話/密碼/搜尋類型）、
  `<select>`、`<textarea>`，只要沒有被更明確的樣式覆蓋，一律至少
  16px。之後不管系統裡新增什麼輸入框，就算開發時忘記手動設定字級，
  也不會再犯 iOS Safari 自動放大的問題，不用再靠「地毯式搜尋、一個個補」
  這種容易有漏網之魚的做法
- ✅ 順手把分類管理（5個）、單位管理（1個）、手動封鎖信箱（1個）
  共 7 處明確缺少 font-size 的輸入框也直接補上明確數值，雙重保險

## 2026-08-30　全站彈窗背景頁面會跟著滑動的問題

- ✅ **修正所有彈窗（表單/確認對話框/搜尋選擇清單/圖片預覽）開著時，
  背景頁面還可以被滑動的問題**：手機上在彈窗裡滑動找項目時，手指
  劃過背景區域會讓背景也跟著一起動，體驗很不像原生 App。這是全站
  性的問題，因為每個彈窗各自在很多不同地方被關閉（各種確認/取消/
  選擇按鈕），如果一個個去改每個關閉的地方很容易漏掉。改用
  `MutationObserver` 自動偵測彈窗何時從畫面上被移除，統一在
  `modal-ui.js` 新增 `mountOverlay()` 處理鎖定/解鎖，用計數器正確
  處理「彈窗疊彈窗」的情況（例如表單彈窗上面又跳出確認對話框），
  呼叫端完全不用修改自己的關閉邏輯。`openModal`、`confirmDialog`、
  `alertDialog`、`openImageLightbox`（modal-ui.js）跟
  `openSearchPicker`（picker-ui.js，例如商品/客戶選擇清單）都已套用

## 2026-08-30　批次出貨清單加上取貨日期，並自動依日期排序

- ✅ **批次列印出貨清單加上取貨日期**：單張出貨單本來就有顯示「預計
  出貨/取貨日期」，但一次列印多張訂單的批次清單卻沒有，一次印很多張
  混在一起容易搞不清楚哪張是哪天要處理。已加在「取貨方式」欄位下方
  附註顯示，不新增欄位、表格不會變擠
- ✅ **批次列印前自動依取貨日期排序**：不管使用者在列表頁勾選訂單的
  順序為何，列印出來的清單一律先依「預計出貨/取貨日期」由早到晚
  排好，同一天的訂單自然會排在一起，比較好照日期分批處理；沒填日期
  的排在最後面。排序邏輯直接寫在 `printShippingList()` 函式內部，
  之後不管從哪裡呼叫這個列印功能，都會自動套用，不用呼叫端自己記得排序

## 2026-08-30　商品改名後統計報表被拆成兩筆的修正

- ⚠️ **確認：訂單裡的商品名稱本來就是「下訂當下的名稱快照」，不是 bug**：
  跟成本鎖定是同一套設計精神——訂單成立當下的商品名稱會鎖住，之後
  改名不會讓舊訂單/舊出貨單的名稱跟著變動，這是為了維持歷史紀錄的
  準確性（類似發票開出去之後不會因為店家後來改名跟著改）。客戶詳細頁
  的「訂購紀錄」是用客戶的固定 ID 比對，不受商品改名影響
- ✅ **但找到真正的 bug：統計報表/利潤總覽的「依商品」統計是用名稱分組**：
  `reports-ui.js` 的商品銷售排行、`profit-ui.js` 的包材/現貨成本拆解
  （依商品分頁），原本都是用 `li.productName`（下訂當時的名稱快照）
  當作分組的 key，商品改名後，改名前後的訂單會被拆成兩筆不同項目，
  看起來像是這個商品的銷售紀錄斷掉、消失了一部分。已修正為用商品的
  固定 ID 分組，畫面上顯示的名稱優先用商品「目前」的名稱（改名後
  舊訂單的統計數字也會自動顯示新名字），商品已被永久刪除查不到目前
  名稱時，才退回用訂單當時的名稱快照。`profit-ui.js` 的「依包材項目」
  拆解（`byMaterial`）也一併用同樣邏輯修正

## 2026-08-30　新增「待確認」標記功能

- ✅ **訂單可以標記「待確認」，方便找出還沒跟客戶核對清楚的訂單**：
  key單時有些資訊還不確定（例如取貨時間），之前建立後很難再找出這些
  訂單。新增/編輯訂單表單裡加了一個勾選框「這張訂單有資訊還沒確認」，
  勾選後可以附註要確認什麼（例如「取貨時間還沒確定」）
  - 訂單列表卡片、訂單詳細頁的狀態徽章列都會顯示明顯的「待確認」標籤
  - 訂單詳細頁另外有一張醒目的提示卡片，顯示待確認事項內容，
    旁邊有「已跟客戶確認，取消標記」按鈕，一鍵清除不用重新編輯整張訂單
  - 訂單列表的快速篩選新增「待確認資訊」選項，可以直接篩選出這些訂單，
    也跟其他需要看全部歷史的篩選一樣，會自動展開抓取全部歷史資料，
    避免超過3個月的舊訂單被漏掉
  - 新增 `js/orders.js` 的 `updateConfirmationStatus()`，資料庫層面
    不需要新增 Firestore 規則（訂單集合本來就允許這些角色寫入）

## 2026-08-30　新增「備料清單」功能

- ✅ **新增備料清單頁面**（`js/prep-ui.js`），側邊欄「營運」分類，
  回答「我要準備多少原料」這個問題：
  - 彙總所有「還沒出貨」的訂單，算出每個商品總共要準備幾份
  - 自製商品會自動展開配方，算出包材/原料的總用量
  - 包材/原料需求會跟目前庫存比對，直接顯示「還缺多少」，
    庫存夠的話顯示「夠用」，不用自己心算
  - 可以選填「預計出貨/取貨日期」區間，只看特定時間範圍要準備的量
    （例如只看這週要出的貨），不填就是統計全部還沒出貨的訂單
  - 權限比照商品與庫存頁面（超級管理員/管理員/訂單進出貨人員/唯讀成員）

## 2026-08-30　備料清單篩選區塊排版修正

- ✅ **修正備料清單頁面篩選區塊的排版問題**：原本每個日期欄位各自有
  一條長標籤（「預計出貨/取貨日期（選填）」），搭配左右並排的窄欄位
  導致文字換行三次，加上「清除」按鈕用大按鈕樣式、位置跟欄位對不齊，
  整體看起來很亂。改成單一標籤置頂、日期欄位改固定寬度置中排列
  （比照系統既有的日期區間選擇器視覺風格），「清除」改成文字連結、
  只有真的有篩選日期時才出現，減少不必要的視覺雜訊

## 2026-08-30　備料清單修正兩個邏輯漏洞

使用者詢問「這個有邏輯漏洞嗎」，重新檢視後找出兩個真正的問題並修正：

- ✅ **現貨商品原本沒有跟庫存比對缺口**：商品需求表格原本只顯示「需要
  準備幾個」，現貨商品（resale，例如進貨轉賣的東西）明明有庫存追蹤，
  卻沒有像包材那樣顯示「目前庫存」「還缺多少」，畫面上看不出現貨
  庫存夠不夠賣。已修正成商品需求表格也加上這兩欄，跟包材/原料需求
  表格用同一套邏輯；自製商品做多少賣多少、沒有庫存概念，這兩欄顯示「—」
- ✅ **跟「待確認」訂單沒有互動**：備料清單原本一視同仁把「待確認」
  （資訊還沒跟客戶核對清楚）的訂單也算進總需求裡，如果客戶之後改單，
  可能已經照數字準備原料、白忙一場。已修正成每項商品/包材需求，
  如果其中有部分來自待確認的訂單，會用小字附註「其中 X 個來自尚待
  確認的訂單」，讓使用者自己評估要不要先準備那個部分

## 2026-08-30　新增「巢狀配方」支援：禮盒可以裝其他自製商品

使用者發現備料清單有個沒算到的地方：禮盒這種組合商品，配方只能選
「包材」，系統完全不知道禮盒裡面裝了幾顆蛋黃酥，導致備料清單算出來
的單顆商品需求量是錯的（只算了客人單獨訂購散顆的量，沒算進裝進禮盒
裡的量）。同時也發現這連帶影響禮盒本身的成本/毛利計算，因為配方
只算包材成本，完全沒算進裡面裝的蛋黃酥成本，毛利數字被高估。

- ✅ **配方現在可以選「其他自製商品」，不只是包材**：新增/編輯自製商品
  時，配方欄位開放選擇其他自製商品（例如禮盒配方可以加「蛋黃酥 x6」），
  自動排除自己避免最單純的自我引用
- ✅ **成本計算支援遞迴**：`calcItemCost()` 遇到配方裡是自製商品的
  項目，會遞迴算出那個商品自己的成本，禮盒的毛利數字會因此變得更
  準確（但也會比之前看到的數字更低——因為之前根本沒算進去）。加上
  循環引用偵測（A用到B、B又繞回用到A），偵測到會安全停止並顯示警告，
  不會讓網頁卡死
- ✅ **出貨扣庫存／退貨回補都改用完整展開邏輯**：新增共用函式
  `js/items.js` 的 `expandRecipe()`，配方裡如果引用到自製商品，
  會繼續往下展開到最終的包材為止，`markShipped`（出貨扣庫存）跟
  `registerReturn`（退貨回補）都改用這個共用函式，確保邏輯一致
  （原本如果不修，出貨時系統會誤把自製商品當包材去扣庫存，
  自製商品沒有庫存概念，會直接報錯「庫存不足」）
- ✅ **備料清單正確展開巢狀需求**：禮盒展開後，裡面裝的單顆商品需求
  會正確加進「商品需求」表格（跟客人直接訂購散顆的量合併計算），
  包材需求也會正確累加進「包材/原料需求」表格
- 💡 **影響範圍說明（已告知使用者）**：只影響「之後」的訂單，比照
  系統一貫的成本鎖定精神，已出貨的舊訂單毛利不會被回頭改掉；出貨
  扣庫存邏輯不受影響（自製商品本來就沒有庫存，扣的一直都是包材）

## 2026-08-30　加到主畫面後登入按鈕沒反應的修正

- ✅ **修正加到主畫面（PWA 獨立模式）點 Google 登入完全沒反應的問題**：
  iOS 的獨立模式對跳出彈跳視窗登入的支援不好，常常會完全沒有任何
  反應——連錯誤都不會回報，導致原本「跳窗失敗自動改用整頁轉跳」的
  容錯邏輯根本沒機會被觸發（因為它是靠偵測到錯誤才啟動）。已修正為
  一開始就先判斷「現在是不是在獨立模式下開啟」，是的話直接跳過彈跳
  視窗，一開始就用整頁轉跳登入，不嘗試跳窗
- 💡 **提醒使用者**：獨立模式（加到主畫面的捷徑）跟一般 Safari 分頁
  是不同的儲存環境，新加入主畫面的第一次開啟需要重新登入一次是正常
  現象，不是 bug；登入過一次之後，之後從主畫面圖示開啟應該會維持
  登入狀態，不用每次都重新登入

## 2026-08-30　緊急修正：items.js 語法錯誤導致整個網站掛掉

- 🔴 **嚴重事故**：稍早新增「巢狀配方」功能時（`expandRecipe` 函式），
  插入程式碼的過程不小心切斷了下面 `restockFromReturn` 函式的註解
  開頭 `/**`，留下一段變成「裸露程式碼」的中文註解文字（沒有被
  `/* */` 包住），瀏覽器把這段文字當成真正的程式碼去解析，直接
  在 `Unexpected token '*'` 報錯，導致 `items.js` 整個載入失敗，
  牽連整個網站（包含登入頁）都打不開
  - 本地用 `node --check` 檢查工具**沒有**在事前抓到這個問題
    （原因待查，之後會留意這種「先插入新程式碼、再確認周邊註解
    區塊有沒有被切斷」的檢查）
  - 已修正：補回被切斷的 `/**` 開頭，註解區塊配對恢復正常，
    全站所有檔案已重新逐一跑過 `node --check` 語法檢查確認無誤

## 2026-08-30　修正同一天訂單排序看起來隨機的問題

- ✅ **同一天的訂單現在會依建立先後順序排列**：訂單列表原本只依
  `orderDate`（只精確到「日期」，不含時間）排序，同一天有好幾筆訂單
  時，Firestore 在這個欄位相同的情況下不保證任何特定順序，看起來
  就像隨機排列，難怪使用者覺得「亂」。已修正為在瀏覽器收到資料後，
  用 `createdAt`（精確到秒的建立時間）補做第二層排序，同一天的訂單
  會照建立先後順序排好
  - **刻意不直接把這個排序需求寫進 Firestore 查詢本身**：那樣做
    （用兩個 `orderBy` 欄位）需要額外的資料庫複合索引，沒有先建好
    索引就部署的話，會讓整個訂單查詢直接失敗——剛經歷過語法錯誤讓
    整站掛掉的事故，這次特別注意不要冒這個風險，改成在瀏覽器端排序，
    完全不需要任何額外的資料庫設定

## 2026-08-30　作廢訂單不再同時顯示矛盾的狀態徽章

- ✅ **修正作廢訂單畫面上同時顯示「待處理/未收款/待確認/已作廢」四個
  徽章的問題**：訂單一旦作廢，出貨狀態、收款狀態、待確認狀態就都
  已經沒有意義了，同時顯示反而讓人搞不清楚訂單到底是什麼狀態。
  已修正為作廢的訂單只顯示「已作廢」一個徽章，其他徽章都不再顯示；
  「這張訂單還有資訊要跟客戶確認」的提示卡片，作廢後也不再出現。
  訂單列表卡片跟訂單詳細頁都已修正，列表卡片原本訂單編號旁邊多餘的
  「(已作廢)」文字也拿掉了，統一用徽章呈現

## 2026-08-30　列印出貨單/清單加上待確認提示

- ✅ **列印時會顯示待確認事項**：使用者需要把訂單印出來，一個一個
  跟客戶當面/電話確認，原本列印出來的單子完全看不出哪些訂單還在
  待確認狀態、要確認什麼。已修正：
  - **單張出貨單**：待確認的訂單會在上方多一個醒目的黃色提示框，
    顯示「⚠ 這張訂單還有資訊要跟客戶確認」跟具體的待確認事項內容
  - **批次出貨清單**：待確認的那一列會有淡黃色底色標示，掃視整張
    印出來的紙就能一眼找到，訂單資訊下方也會附註「⚠ 待確認：...」
  - 特別加上 `-webkit-print-color-adjust: exact` 設定，確保背景顏色
    真的會被印出來，不會被瀏覽器的省墨模式自動拿掉變成純白色

## 2026-08-30　批次出貨清單改成A4橫向列印

- ✅ **批次出貨清單改成橫向列印**：訂單數量多、欄位多的時候，直向 A4
  版面太窄，各欄位容易被壓縮擠在一起。已加上 `@page { size: A4
  landscape; }` 設定，改成橫向，每一欄能有更多空間。單張出貨單維持
  直向不變（欄位單純、直向比較符合單張紙的閱讀習慣）

## 2026-08-30　配方可以排除子項目自己的包材

使用者發現巢狀配方一個真實情境：蛋黃酥裝進禮盒是散裝的，不需要單顆
蛋黃酥自己的小包裝（例如貼紙），但系統原本會把蛋黃酥自己的包材成本／
庫存也一併算進禮盒裡，造成重複計算跟出貨時誤扣不會用到的包材庫存。

- ✅ **配方列新增「不計入這個子項目自己的包材」勾選框**：只有選到
  自製商品的那幾列才會出現。勾選後：
  - **成本計算**：`calcItemCost()` 遇到這一列時，會把那個子項目自己
    配方裡的包材成本算成 $0（子項目底下如果又巢狀用到其他自製商品，
    各自獨立判斷，不會被這個設定強制連坐）
  - **出貨扣庫存／備料清單**：`expandRecipe()` 同步套用同一套邏輯，
    出貨時不會誤扣那個包材的庫存，備料清單也不會虛報需求量
  - 商品詳細頁的成本拆解表格會顯示「（已排除，不列入本商品成本）」
    的附註，清楚看出這筆是刻意排除的，不是漏填
- 編輯既有商品時，這個設定會正確保留（原本這裡有個小疏漏：
  重新開啟編輯畫面會遺失這個設定，一併修正）

## 2026-08-30　新增「重算待處理訂單成本」功能（超級管理員）

使用者剛開始用系統、已經手動打了 50 幾筆訂單，這次巢狀配方功能上線
後想知道要不要重打訂單才能套用新的成本邏輯。討論後確認：

- **備料清單完全不用重算**——它是即時用商品目前的配方展開計算的，
  只要把商品配方補上，所有還沒出貨的訂單備料清單馬上就會正確
- **但訂單自己顯示的成本／毛利數字、利潤總覽、統計報表，因為訂單
  成立當下的成本會鎖住，不會自動跟著配方變動更新**

討論範圍後決定：**只重算「還沒出貨」的訂單**（已出貨的訂單因為已經
真的扣過庫存，回頭改成本容易讓庫存帳對不起來，風險較高，刻意排除）。

- ✅ **新增 `js/orders.js` 的 `recalcPendingOrderCosts()`**：抓出所有
  還沒出貨、沒作廢的訂單，用商品目前的配方重新算 `unitCost` /
  `costBreakdown`，不動 `qty`/`unitPrice`/`subtotal`/`totalAmount`
  等售價相關欄位（客戶要付的金額完全不受影響）。個別商品如果已經
  找不到（可能被永久刪除），該項目保留原本的舊成本、不會讓整筆
  訂單失敗，會回報是哪幾筆；配方如果偵測到循環引用也會回報
- ✅ **新增側邊欄「超級管理員」分類下的「重算訂單成本」頁面**
  （`js/recalc-ui.js`）：先顯示目前有幾筆符合條件的訂單，執行前
  需要輸入「確認」兩個字（比照作廢/永久刪除的二次確認精神），
  執行後列出更新筆數、找不到商品的訂單清單、循環引用警告清單

### ⚠️ 過程中的自我檢討：又犯了兩次跟稍早一樣的失誤

這次新增 `recalcPendingOrderCosts()` 函式時，插入程式碼的過程**又一次**
不小心切斷了旁邊 `listReturnsByOrder` 函式的定義（跟稍早 `items.js`
那次一模一樣的失誤模式：插入新函式時，把緊接在後面的函式簽名行
`export async function xxx(...) {` 誤刪，只留下函式主體變成裸露程式碼）。
這次因為裸露的程式碼剛好還算合法語法（不是註解符號配對問題），
不會在儲存時被語法檢查抓到，而是會在網頁載入時試圖執行這段殘留
程式碼、噴出「orderId 未定義」的執行期錯誤，一樣會讓整個網站掛掉。

已經在部署前發現並修正，同時對全站做了「函式呼叫端跟定義端交叉比對」
檢查，確認每個被使用的函式都真的有定義存在，這次沒有讓錯誤上線。
之後每次用「插入程式碼」的方式新增函式，都會養成習慣：插入後立刻
用 `grep -n "^export"` 列出所有函式定義，跟修改前的清單比對數量，
確認沒有函式在插入過程中被意外吃掉。

## 2026-08-30　配方勾選框文字精簡

- ✅ **「不計入子項目自己的包材」勾選框文字太長，精簡成「散裝，不算
  它自己的包材」**：原本的文字把完整說明都塞在勾選框旁邊，畫面看
  起來很複雜。因為使用者主要用手機操作，滑鼠停留才顯示的提示文字
  （tooltip）在手機上沒有用武之地，所以沒有另外加提示圖示，單純
  精簡文字本身

## 2026-08-30　備料清單排版改善 + 禮盒拆解說明

- ✅ **排版改成卡片式列表，取代擁擠的表格**：原本 4 欄位的表格
  （商品/需要準備/目前庫存/還缺）在手機上標題文字會被擠壓換行，
  看起來很雜亂。改成每個項目一張卡片，名稱跟需求量放同一行，
  庫存/缺口資訊放下面一行，手機閱讀起來更清爽
- ✅ **加上「這個數量有多少來自禮盒拆解」的說明**：使用者擔心禮盒
  展開後，商品需求表格裡同時出現「禮盒」跟「裡面裝的單顆商品」
  會不會搞混。已加上兩個提示：
  - 禮盒這種組合商品本身的那一列，會標註「內含其他商品，已分開列出」
  - 被拆解出來的單顆商品，會標註「其中 X 個是從禮盒等組合商品拆解
    出來的」，清楚知道這個數字有一部分不是客人直接單獨訂購的

## 2026-08-30　常見問題頁面 + 版本更新紀錄 + 選單分類改名

- ✅ **側邊欄「營運」改名為「日常」**，比較口語化
- ✅ **新增「常見問題」說明頁面**（`js/faq-ui.js`），側邊欄所有角色
  都看得到，放在首頁之後、日常分類之前。內容依角色自動篩選——每個
  章節、每一則問答都可以個別設定「哪些角色看得到」，例如「訂單進
  出貨人員」看不到「怎麼作廢訂單」的說明（因為那個角色本來就沒有
  這個權限），避免看到用不到的功能說明造成困擾。內容涵蓋訂單管理、
  商品與庫存、備料清單、客戶與廠商、統計報表/利潤總覽、超級管理員
  專區，共六個章節，附搜尋功能
- ✅ **新增「版本更新紀錄」**：側邊欄底部登出按鈕下方，有一個低調的
  小文字連結「版本更新紀錄」，點開會用彈窗顯示簡易版更新內容（用
  一般使用者看得懂的語言，不是技術細節），依日期由新到舊排列。
  之後每次有新功能上線，在 `js/app.js` 的 `CHANGELOG` 陣列最前面
  加一條新的即可

## 2026-08-30　常見問題/版本紀錄微調

- ✅ **拿掉常見問題頁面底部「內容已依角色篩選」的提示文字**：使用者
  覺得這句話反而會讓人意識到「還有其他角色能看到更多內容」，感覺
  怪怪的，直接拿掉，篩選機制照常運作，只是不再特別講出來
- ✅ **版本更新紀錄只給管理員以上看**：一般成員（訂單進出貨人員、
  唯讀成員）不需要知道系統背後做了哪些技術調整，側邊欄底部那個
  連結現在只有超級管理員跟管理員看得到

## 2026-08-30　「訂單進出貨人員」角色改名為「小幫手」

- ✅ **角色名稱從「訂單進出貨人員」改成「小幫手」**：使用者覺得原本
  的名稱太長、偏正式，跟品牌溫暖手作的調性不搭。改在 `js/auth.js`
  的 `ROLE_LABELS` 統一調整，全站顯示（側邊欄、成員管理、常見問題
  頁面等）都會自動套用新名稱。`firestore.rules` 裡有兩處提到舊名稱
  的地方只是內部註解，不會顯示給任何人看，不影響實際權限運作，
  不需要重新發布規則

## 2026-08-30　自動提醒設定暱稱

使用者反映有些成員一直沒設定暱稱，畫面上顯示英文/數字帳號，根本不知道
是誰在操作，靠成員自己想起來去改效果不好。

- ✅ **登入後如果還沒設定暱稱，自動跳出提示視窗**：`openProfileModal`
  新增 `mandatory` 模式，會在視窗上方顯示醒目提示「還沒有設定暱稱」，
  並要求暱稱欄位不能是空白才能儲存。使用者還是可以關掉視窗跳過，
  但下次登入還是會再跳出來，直到真的設定為止——不是完全鎖死不能用，
  是持續、有效的提醒，比等他們自己想到有效很多
- ✅ **成員管理頁面加上醒目標示**：原本就有顯示「（尚未設定暱稱）」
  文字，但顏色跟一般文字一樣不夠顯眼。改成紅色顯示，超級管理員一眼
  掃過去就能看出誰還沒設定，方便私下直接提醒

## 2026-08-30　暱稱提醒文字修正

- ✅ **簡化「還沒設定暱稱」提醒文字**：原本文字提到「操作紀錄、收款
  紀錄」，但「操作紀錄」是超級管理員限定的功能，一般成員根本看不到、
  不知道有這個東西，講出來反而讓人一頭霧水。改成單純「讓大家知道是
  誰在操作」，不特別提及任何看不到的功能

## 2026-08-30　通知鈴鐺補上待確認統計，已出貨的訂單不再提醒

- ✅ **通知鈴鐺加入「待確認」統計**：之前做「待確認」標記功能時忘記
  加進通知鈴鐺，現在鈴鐺會提醒有幾張訂單還在等確認，點下去會導向
  訂單列表並自動套用待確認篩選
- ✅ **已出貨的訂單不再提醒待確認**：東西都已經寄出去了，不管當初
  有沒有確認清楚，再提醒也於事無補。已同步套用到四個地方，確保
  邏輯一致（不會出現「鈴鐺說3張、點進去卻看到5張」的矛盾）：
  - 通知鈴鐺的統計數字
  - 訂單列表「待確認資訊」篩選
  - 訂單列表卡片的待確認徽章
  - 訂單詳細頁的待確認徽章與黃色提示卡片（連帶「已跟客戶確認，
    取消標記」按鈕也會一起消失，因為已經沒有東西需要手動清除了）
  - 資料庫裡的 `needsConfirmation` 欄位本身不會被清掉，只是出貨後
    畫面上不會再顯示，保留歷史紀錄但不打擾使用者

## 2026-08-30　暱稱提醒再精簡 + 列印功能補上一致性

- ✅ **暱稱提醒拿掉整段說明文字**：只留「還沒有設定暱稱」這句標題，
  下面的解釋文字整段拿掉，使用者覺得原本的說明看起來很奇怪、不需要
- ✅ **確認並補上「待確認」邏輯的一致性**：
  - 已出貨的訂單本來就沒有「編輯訂單」的入口（編輯按鈕只有還沒出貨
    才會出現），不會有進去編輯還看得到待確認勾選框的情況，這點確認
    沒問題
  - 但列印功能（單張出貨單、批次出貨清單）原本沒有排除已出貨的訂單，
    如果選到已出貨的訂單去列印，待確認提示還是會跑出來，跟其他地方
    （鈴鐺、列表、詳細頁）的邏輯不一致。已修正，出貨後列印也不會再
    顯示待確認提示

## 2026-08-30　成員管理新增「最後上線時間」

- ✅ **成員管理頁面加上「最後上線時間」顯示**：每次登入成功時，會
  自動記錄一個時間戳記到成員資料裡（不等寫入完成才放行，避免拖慢
  登入速度）。畫面上用好讀的相對時間顯示（剛剛上線/X分鐘前/X小時前/
  X天前），超過30天沒上線的話會改用紅色標示，方便超級管理員一眼
  看出誰已經很久沒用系統了
- ⚠️ **需要使用者操作**：這次為了讓成員能自己寫入這個新欄位，修改了
  `firestore.rules`（v6，開放成員自我更新時多一個 `lastLoginAt` 欄位，
  原本只能改 `nickname`）。**需要把更新後的規則貼到 Firebase Console
  並發布**，這個功能才會真的生效，不然寫入時會被規則擋下來

## 2026-08-30　找到並修正加到主畫面登入卡住的真正原因

使用者堅持指出：同一支手機、同一個 Google 帳號，另外兩個 App（庫存
管理APP、嗶啵）都能在「加到主畫面」的獨立模式下正常登入，只有這個
系統不行。這代表問題不是「iOS 通用限制」，是這個系統本身設定有問題，
不該歸咎給使用者或籠統的平台限制。

- ✅ **加上可見的診斷訊息**：登入前先記一個旗標，轉跳回 Google 登入
  完成後，不管結果如何都會跳出明確的診斷 toast（成功會顯示帳號
  email，失敗會顯示警告），讓使用者能截圖回報，不再是使用者完全
  看不出發生什麼事的靜默失敗
- ✅ **實際測試結果**：使用者截圖顯示「⚠️ 登入轉跳沒有帶回帳號資訊」，
  證實轉跳去 Google 登入完成、跳回系統之後，系統完全遺失了登入結果
- ✅ **找到根本原因並修正**：系統原本沒有明確指定 Firebase「登入狀態
  要用什麼方式記住」，讓 Firebase SDK 自動判斷，懷疑自動判斷的時機
  剛好跟「轉跳去 Google 登入」的動作卡在一起，導致轉跳回來後系統
  認不出剛剛登入過。已修正為：
  - 在 `firebase-config.js` 明確指定用 `indexedDBLocalPersistence`
    （不支援的話退回 `browserLocalPersistence`）
  - 登入流程（`loginWithGoogle`）改成明確等待這個設定真的生效完成，
    才進行轉跳，消除任何時間競速的可能
- 💡 **這次的排查方式值得記錄**：靠對話搜尋工具找到之前幫使用者做過
  的其他兩個 App（庫存管理APP、嗶啵）的開發紀錄，比對出它們也踩過
  同樣的坑、以及最後怎麼處理的，這比憑空猜測有效很多。使用者堅持
  「同一支手機另外兩個App可以，這個不行」這個關鍵線索，成功把問題
  方向從「歸咎於平台限制」導正到「系統本身的設定問題」，是這次能
  找到根因的關鍵轉折點

## 2026-08-30　訂單列表新增「已作廢」篩選

- ✅ **狀態篩選下拉選單新增「已作廢」選項**，限管理員以上（超級管理員/
  管理員）才看得到。使用者提出：作廢訂單如果要重打，需要能快速找到
  之前作廢那筆的資料當參考，原本作廢的訂單雖然混在「全部狀態」列表
  裡（用徽章標示），但沒有專門的篩選方式快速找出來
  - 選「待處理」「已出貨」這兩個篩選時，作廢的訂單不會再混進來
    （即使 shipStatus 欄位剛好符合，作廢了就不算數），語意更清楚
  - 「已作廢」篩選也加入自動展開全部歷史的保護，避免超過3個月的
    舊作廢訂單被漏掉

## 2026-08-30　修正一般Safari登入跳出瀏覽器彈出視窗詢問

- ✅ **修正一般 Safari 分頁登入時，會跳出「網站正嘗試打開彈出式視窗」
  瀏覽器原生詢問的問題**：上次為了測試獨立模式登入，把「一律先嘗試
  跳窗」套用到全部情境，結果連一般 Safari 分頁的正常登入也被波及，
  多了這個不必要、容易讓人誤會的原生詢問。改回單純用整頁轉跳，不再
  嘗試跳窗——獨立模式現在已經有自己專屬的引導畫面（見上一則紀錄），
  根本不會走到這個登入函式，`loginWithGoogle()` 現在只服務一般瀏覽器
  分頁，整頁轉跳從頭到尾都運作得很穩定，不需要多此一舉先試跳窗
- 順手清掉不再使用的 `signInWithPopup`、`POPUP_FALLBACK_CODES`、
  `isStandaloneMode` 相關程式碼，保持乾淨

## 2026-08-30　管理員作廢訂單需要超級管理員確認

- ✅ **管理員（不是超級管理員自己）作廢訂單時，會標記給超級管理員
  確認不是誤觸**：超級管理員自己作廢的不用再確認自己，只有「管理員」
  這個層級的作廢動作才會觸發
  - `voidOrder()` 記錄作廢當下操作者的角色 `voidedByRole`，管理員
    作廢的話設定 `needsVoidReview: true`
  - 新增 `acknowledgeVoidReview()`，超級管理員可以一鍵標記「確認無誤」
  - 通知鈴鐺加入統計（超級管理員限定），點下去導向訂單列表並套用
    「作廢待確認」篩選
  - 訂單列表狀態篩選新增「作廢待確認」選項（超級管理員限定）
  - 訂單列表卡片、詳細頁都會顯示「待確認」標示；詳細頁另外有一張
    黃色提示卡片顯示是誰作廢的，附「確認無誤」按鈕
  - 這次不需要修改 Firestore 規則（超級管理員/管理員本來就有訂單
    完整寫入權限），不用重新發布規則

## 2026-08-30　獨立模式改用 Google Identity Services 登入

嘗試過的方向（popup、redirect、各種持久化設定組合）在使用者的獨立模式
環境下都沒有成功，這次改用完全不同的技術路線。

- ✅ **新增 Google Identity Services（GIS）登入方式，只在獨立模式下
  啟用**：不透過 Firebase 的 `signInWithPopup`/`signInWithRedirect`，
  改用 Google 官方的登入元件（`accounts.google.com/gsi/client`），
  拿到憑證後再用 `signInWithCredential()` 交給 Firebase 完成登入
  - `index.html` 載入 GIS 官方腳本，獨立模式下顯示 GIS 渲染出來的
    官方登入按鈕（取代之前的「複製網址」引導畫面）
  - `js/auth.js` 新增 `signInWithGoogleCredential(idToken)`，接收
    GIS 拿到的 id_token 換成 Firebase 登入憑證
  - `js/app.js` 負責偵測獨立模式、等待 GIS 腳本載入完成（輪詢，
    最多等8秒）、初始化並渲染按鈕、串接登入結果
  - **一般 Safari 分頁完全不受影響**，維持原本正常運作的登入方式
    不變，這次改動刻意只影響獨立模式這條路徑，降低風險
- ⚠️ **潛在風險（尚待實測驗證）**：GIS 需要網站網域在 Google Cloud
  Console 的 OAuth 用戶端「已授權的 JavaScript 來源」清單裡，這個
  清單跟 Firebase 的「已授權網域」不完全是同一份設定，如果沒有自動
  同步過去，GIS 初始化可能會失敗。如果測試後發現按鈕出不來或報錯，
  下一步要去確認這個設定

## 2026-08-30　新增加到主畫面教學彈窗

- ✅ **第一次登入自動跳出「加到主畫面」教學**，整合進原本提醒設定
  暱稱的那個彈窗一起顯示：
  - 新增 `member.hasSeenHomeScreenTip` 欄位，跟暱稱獨立判斷——沒設定
    暱稱、或沒看過這個教學，任一條件成立就會跳出彈窗，各自只顯示
    對應需要的區塊
  - 教學內容包含 iPhone（Safari）跟 Android（Chrome）兩種平台的步驟
  - 不管是按「儲存」還是直接關掉彈窗，都會標記「已看過」，之後不會
    再自動跳出來
  - **目前既有的帳號（不管有沒有設定過暱稱）都還沒有這個欄位**，
    所以全部都會被當成「第一次」看到這個教學一次，符合需求
  - 常見問題頁面新增「基本使用」章節，收錄同樣的教學內容，之後想
    回頭查也找得到
- ⚠️ **需要使用者操作**：`firestore.rules` 更新為 v7（成員自我更新
  多開放 `hasSeenHomeScreenTip` 欄位），需要重新貼到 Firebase Console
  發布才會生效

## 2026-08-30　主畫面教學拆成獨立彈窗

- ✅ **「加到主畫面」教學拆成獨立彈窗，不再跟暱稱設定塞在同一個彈窗
  裡**：使用者反映已經設定過暱稱的人，突然又跳出「還要做什麼」的
  內容會很突兀。改成：
  - `openProfileModal()` 恢復成單純只處理個人資料/暱稱，跟之前一樣
  - 新增獨立的 `openHomeScreenTipModal()`，只顯示加到主畫面的教學，
    有自己的「知道了」按鈕
  - 登入時的判斷邏輯：**暱稱還沒設定的話，這次登入只會看到暱稱提醒**
    （教學彈窗不會同時跳出來），等暱稱存檔（會整頁重新整理）之後，
    下次載入才會接著看到教學彈窗；**暱稱已經設定好、只是還沒看過
    教學的人**，才會直接單獨跳出教學彈窗

## 2026-08-30　作廢訂單一律顯示明確確認狀態徽章

- ✅ **所有已作廢的訂單，一律顯示明確的確認狀態徽章**：原本只有
  「待確認」（管理員作廢、還沒被確認）才會顯示徽章，超級管理員自己
  作廢的訂單完全沒有任何標示，使用者反映這樣沒辦法一眼看出「這張
  是已確認還是待確認」，還要點進去才知道。已修正為：
  - 管理員作廢、還沒被確認 → 顯示「⚠ 待確認」（金色）
  - 超級管理員自己作廢，或管理員作廢但已經被確認過 → 顯示「✓ 已確認」
    （綠色）
  - 訂單列表卡片、訂單詳細頁都已套用，不用點進去就能一眼分辨

## 待辦／待確認清單（尚未處理）

1. 🔜 **需要使用者操作**：把 `firestore.rules` 貼到 Firebase Console 並發布，退貨功能才會真的生效
2. 💬 首頁統計/通知鈴鐺改用 Firestore count()聚合查詢 — **使用者決定先擱置**（需資料結構異動+補舊資料，成本較高，目前不感覺變慢）
2. 💬 商品清單分頁化 — 優先度較低
3. ⚠️ GitHub Token 撤銷更換 — 待使用者自行處理
4. ⚠️ Cloudinary unsigned preset 加限制 — 待使用者自行處理
5. 💬 版本號部署流程自動化 — 待決定是否要做
